import React from 'react'

import {
  AppBar,
  Toolbar,
  Typography
} from '@mui/material'

const TitleBar = () => {
  return (
    <AppBar>
      <Toolbar>
        <Typography>管理台帳</Typography>
      </Toolbar>
    </AppBar>
  )
}

export default TitleBar
